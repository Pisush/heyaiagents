# Secrets / environment
.env
.env.*
!.env.example

# Durable state (the leaderboard JSON file)
/leaderboard.json
*.leaderboard-*.tmp

# Standalone Tailwind CLI binary (download per-machine — see README)
/bin/

# Go build artifacts
/server
*.test
*.out

# Editor / OS
.DS_Store
*.swp
